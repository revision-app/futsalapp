# 試合結果記録機能 要件

## この文書について

既存の成績集計アプリに「試合結果記録機能」を追加したい。出欠登録・MVP登録などのベース機能とデータ構造はすでに存在する。この文書は実装にあたっての要件（何が欲しいか、現場がどう運用しているか）のみをまとめたもので、具体的なテーブル設計・画面実装は実装側に委ねる。

---

## 1. 背景：現場の運用

- チームの活動では、参加メンバーで紅白戦（チームを2つに分けた試合）を行う。
- 試合結果は現在、**紙に「正の字」で、参加メンバー全員がその場で記録**している。
- この紙の運用には次の良さがあり、アプリでも同等を実現したい。
  - 全員が同じ1枚を見ているので、誰かが書き間違えてもその場で気づける。
  - 1点ずつ書き足していくので、記録の経過が見える。
- 一方、各自がバラバラに記録すると食い違いが起き、後から原因を特定できないのが課題。これを防ぎたい。

---

## 2. 記録したいデータの構造

試合は次の階層になっている。

- **活動日**：1日の活動（既存データにあるはず）
- **セッション**：1日の中の「その1」「その2」など。チーム編成を組み替えて複数回行うことがある。
- **試合**：1セッションの中で、同じ編成のまま複数試合を行う。
- 各試合ごとに記録するもの：
  - スコア（チーム1 ○ 対 ○ チーム2）
  - 各チームのゴールキーパー
- セッションごとに記録するもの：
  - 誰がどちらのチームに所属したか（セッション内は編成固定）
  - 各選手のゴール数・アシスト数

---

## 3. 入力に関する要件

### 3.1 個人スタッツの入力単位
- **ゴール・アシストはセッションごとの合計で入力**する（試合ごとに分けて入力する必要はない）。
  - 理由：旧来の運用でもゴール・アシストはセッション合計でしか記録しておらず、試合単位の細かい記録は不要。
- スコアとゴールキーパーは試合ごとに記録する。

### 3.2 正の字方式（重要）
- ゴールは「数値を後からまとめて入力」ではなく、**1点入るごとに1件ずつ記録を追加**していく方式にしたい（紙の正の字と同じ感覚）。
- 追加したゴールは、誰が・どの試合の・誰のアシストかが分かる形で残す。
- 間違えたら、その1件だけ取り消せること。取り消しても「いつ取り消したか」の痕跡が残るのが望ましい（齟齬の原因追跡のため）。

### 3.3 全員リアルタイム共有（重要）
- セッション中、参加メンバー全員が同じ画面を開き、**誰かが記録を追加したら全員の画面に即座に反映**されるようにしたい。
- これにより「全員で同じ正の字を見ている」状態を再現し、その場で間違いに気づけるようにする。

### 3.4 確定の仕組み
- セッション中は記録を自由に追加・修正できる「下書き」状態とする。
- セッション終了時に内容を確認して「確定」でき、確定後は成績集計の対象になる。
- 確定操作は管理ユーザーのみ行える。確定後は管理ユーザーのみ変更可能。

### 3.5 入力者
- 参加メンバーの誰でも記録を追加できるようにしたい（記録係1人に限定しない）。紙を全員で囲んで書く運用に合わせる。

### 3.6 入力の省力化
- 選手を選ぶ場面では、その活動日に「参加」している選手を優先的に候補に出す（既存の出欠データと連携）。
- 勝敗・勝ち点・各種率などは入力させず、スコアや所属から自動で計算する。人が同じ数字を二重入力しないようにする。

---

## 4. 集計・表示に関する要件

入力された試合データから、以下を自動算出して表示したい。手作業での転記は不要にする。

### 4.1 計算ルール（旧来の集計方法を踏襲）

| 指標 | 計算式 | 備考 |
|---|---|---|
| 勝ち点 | 勝ち数 × 3 ＋ 引分け数 × 1 | 「勝ち」はその選手の所属チームが勝った試合数 |
| 平均勝ち点 | 勝ち点 ÷ 試合数 | |
| セーブ率 | GK時失点 ÷ GK回数 | 1試合あたり平均失点。小さいほど良い |
| 得失点 | チーム得点合計 − チーム失点合計 | |
| 1試合あたり総得点 | チーム得点合計 ÷ 試合数 | |
| 1試合あたり総失点 | チーム失点合計 ÷ 試合数 | |
| 参加率 | 参加日数 ÷ 全活動日数 | 出欠データと連携 |
| 勝率 | 勝ち数 ÷ 試合数 | |
| 負け率 | 負け数 ÷ 試合数 | |
| Gぽいん | ゴールを記録した試合の数 | 合計ゴール数ではなく「何試合でゴールしたか」 |
| Aぽいん | アシストを記録した試合の数 | 合計アシスト数ではなく「何試合でアシストしたか」 |

割り算はすべてゼロ除算を回避し、該当なしは「-」表示とする。

### 4.2 失点は2種類を区別する（重要）
- **GK時失点**：その選手がGKを務めた試合で取られた点。セーブ率の計算に使う。
- **チーム失点**：その選手の所属チームが取られた点。得失点差の計算に使う。
- この2つは別物なので混同しない。

### 4.3 表示したいもの

**個人成績一覧（年間通算）**
選手ごとに以下を一覧表示する。
- 参加日数・参加率
- 試合数・勝ち・負け・引分け・勝率・負け率
- ゴール数・アシスト数・Gぽいん・Aぽいん
- GK回数・GK時失点・セーブ率
- チーム得点・チーム失点・得失点・1試合あたり総得点・1試合あたり総失点
- 勝ち点・平均勝ち点

**日別（セッション別）成績**
セッションごとに上記と同じ項目を表示する。

**ランキング**

RANK1系（個人技術系）:
- ゴールランキング（降順）
- アシストランキング（降順）
- キーパーランキング（セーブ率・昇順＝小さいほど上位）

RANK2系（チーム貢献系）:
- 勝ち点ランキング（降順）
- 得失点ランキング（降順）
- 平均勝ち点ランキング（降順）
- Gぽいんランキング（降順）
- Aぽいんランキング（降順）

各ランキングとも、同順位の場合は背番号の昇順を第2キーとする。

集計は常に最新の状態を表示する（手動で更新ボタンを押す必要がない）。

---

## 5. 確定事項・実装側への申し送り

### 確定済み
- **チーム編成は1セッション内で固定**。セッション開始時に全員の所属（REV1/REV2）を1回決めれば、そのセッション中は変わらない。試合ごとの編成入力は不要。
- **対外試合はない**。試合は常にREV内の2分割（紅白戦）のみ。相手チームや外部選手の扱いは考慮不要。
- **チームカラー**：REV1はオレンジ、REV2は赤。UI全体でこれを一貫して使用する。

### 実装側で判断してよい点
- 既存のデータ構造（選手・活動日・出欠など）とのデータ連携方法。
- ゴール・アシストの「セッション合計入力」と、スコア・GKの「試合ごと入力」を1画面でどうまとめるか。
- 過去の紙/Excelデータの移行は行わない（新しい活動から記録開始）想定だが、後回しで可。
- オフライン時の挙動の丁寧さは実装コストと相談して決める。

---

## 6. 補足：スマホでの利用想定

- 入力は基本スマホで行う（活動現場で使う）。片手・短時間でゴールを追加できる操作性を重視したい。
- 屋外・ネット不安定な環境もあるため、接続が切れても入力が失われず、復帰時に同期されると安心。

---

## 7. 画面イメージ（参考）

セッション入力画面は3タブ構成とする。

**編成タブ**：参加選手を左右スワイプでREV1（オレンジ）/ REV2（赤）に振り分ける。スワイプした選手は各チーム列の下に追加される。振り分け完了後「この編成でセッション開始」でライブ入力タブへ遷移。

**ライブ入力タブ**：
- 上部に試合プルダウン（第1試合〜）とスコア直接入力・GKプルダウン。試合を切り替えるとスコア・フィードが連動して更新される。
- 中央にREV1ゴール・REV2ゴールの大ボタン（チームカラーで色分け）。タップするとポップアップが開き、ゴール選手とアシスト選手をそれぞれプルダウンで選んで確定。
- 下部にその試合のゴール記録フィード。各行に「得点者・アシスト者」を同等の目立ち方で表示し、「修正」「取消」ボタンを横並びで配置。記録の追加は全員の画面にリアルタイム反映される。

**スタッツタブ**：REV1/REV2ごとに選手一覧。ゴール・アシスト・失点・GK回数を全項目自動算出で表示（手入力なし）。「セッションを確定する」ボタン（管理ユーザーのみ）を配置。
